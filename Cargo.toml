[package]
authors = ["Barugon"]
description = "VFR Flight Planner"
edition = "2021"
license = "GPL-3.0-or-later"
name = "fp_app"
version = "0.1.0"

[dependencies]
dirs = "4.0"
eframe = {version = "0.20", features = ["persistence", "wgpu"]}
egui_extras = {version = "0.20"}#, features = ["image"]}
egui_file = "0.7"
gdal = "0.14"

[profile.release]
strip = "symbols"

[profile.dev]
# opt-level = 1
