[package]
authors = ["Barugon"]
description = "VFR Flight Planner"
edition = "2021"
license = "GPL-3.0-or-later"
name = "fp_app"
version = "0.1.0"

[dependencies]
dirs = "5.0"
eframe = {path = "../egui/crates/eframe", features = ["persistence", "wgpu"]}
egui_extras = {path = "../egui/crates/egui_extras"}#, features = ["image"]}
egui_file = {path = "../egui_file"}
gdal = {git = "https://github.com/georust/gdal.git"}

[profile.release]
strip = "symbols"

[profile.dev]
# opt-level = 1
